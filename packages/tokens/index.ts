/**
 * @yd-ds/tokens SSOT entry — auto-derived from token.json
 *
 * Usage (after build):
 *   import { flatTokens, tokenGroups, getToken } from "@yd-ds/tokens/json";
 *
 * Legacy hand-maintained API remains at:
 *   import { brandPrimary, checkboxTokens } from "@yd-ds/tokens";
 *
 * @see docs/token-migration-plan.md
 * @see packages/tokens/scripts/generate-tokens.mjs (future codegen)
 */

export * from "./src/ssot/index";
